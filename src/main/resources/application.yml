micronaut:
  application:
    name: sleepermanager

external:
  api:
    sleeper:
      user:
        root: https://api.sleeper.app/v1/user
        getByUsername: /{username}
      league:
        root: https://api.sleeper.app/v1/user
        getLeagues: /{userId}/leagues/nfl/2020